# BSC Build Roster + Checklist

> Rule: continue the current build; log every new request here; work the queue
> after each build completes. Updated after every build.

## 🛰️ MASTER SYSTEM AUDIT + SOP (2026-06-28) — DONE → full report in docs/SOP_AUDIT.md. DO NOT BUILD until founder confirms sequence.
Audited 9 SOP areas via 4 parallel explorations + live probes. KEEP-WHAT-WORKS confirmed: marketplace/cart/checkout(COD+card PnP)/order-history/tracking/profile, POS(location+inventory+shift+CloudPRNT), phone-order create→approve→cascade, pricelist OCR→live pricing, supplier portal, statements, RLS on orders/costs/credit, role gating (NO access leaks). GAP PHASES (build order): P0 G1 RLS money tables → P1 G2/G3 expense capture+approve, G4 receiving→PO→inventory, G5/G6 phone-order handoff+credit, G18 sign-out, G-pos shift loader → P2 G7 customer-approval queue, G9 commercial side-by-side price, G10 customer statements, G11 add-customer card → P3 G12 catalog OCR+bucket, G14 receiving fields, G15 freezer-temp card, G16 freezer-removal UI, G17 CITES quota → P4 G8 WhatsApp intake, G13 boat self-signup. Recommended first: G1.
Goal: BSC = complete pro ecommerce (Amazon/Walmart standard), zero gaps/orphans/dead-ends, every role sees ONLY its lane, every action auto-flows to next step. Audit 6 roles (retail customer, commercial client, fishing-boat/supplier, goods-supplier portal, cashier location-scoped, founder/cofounder) + 8 end-to-end workflows (incoming goods, phone order, customer signup, expense, supplier pricing OCR, commercial pricing, boat onboarding, retail). For each card/workflow: EXISTS / BUILT-BUT-DISCONNECTED / MISSING + access leaks + dependencies + build order. PROCESSOR RECEIVING BUILD PAUSED per "do not build" (schema verified: rich spinytails_* infra; receiving page exists at /spinytails/receiving — feeds audit).

## 🔐 G1 — RLS MONEY TABLES (started 2026-06-28)
- [x] **G1a** (`docs/security/01-rls-g1a.sql`, ON CLIPBOARD → founder runs). Verify-live changed scope: most money tables ALREADY RLS'd (expenses/invoices/product_costs/payment_webhooks); business_accounts/payment_approvals/bank_transactions don't exist. Real holes = `USING true` public ALL policies. G1a fixes: new `bsc_is_internal_staff()` canonical predicate (covers supplier_handler, which is_staff/is_bsc_staff miss); customers PII leak → self-read(auth_user_id) + staff-read; purchase_orders → internal-staff all + supplier-own (supplier_id). Safe: customers writes all service-role; /account only self-SELECTs customers.
- [x] **G1b** (`docs/security/02-rls-g1b.sql`, ON CLIPBOARD → founder runs). Verify-live: purchase_invoices + invoice_payments have NO supplier_id; supplier portal queries them by `.eq('supplier_id',…)` = NON-EXISTENT column → those tabs ALREADY broken/empty → safe to lock staff-only. Both → bsc_is_internal_staff() only (writes are service-role). Closes forgery + any-token read.
- [ ] **G1b-followup (portal feature bug, not security)** — supplier-portal Invoices/Payments tabs query `supplier_id` on purchase_invoices/invoice_payments which lack that column → always empty. Fix = add supplier_id to purchase_invoices + fix client query + supplier-own SELECT policy. Fold into supplier-portal completion.
- [ ] **G1c (note)** — orders INSERT public = INTENTIONAL Phase-2 guest checkout (orders lockdown memory); separate decision, not a leak-fix.

## 📐 POS/E-COM BLUEPRINT — net-new items (2026-06-28 founder ref; ~80% already in SOP_AUDIT)
Slot into later phases unless founder reprioritizes:
- [ ] N1 Physical SCALE integration (auto weight→price at POS) — hardware. (weight×price for lb items already exists in software.)
- [ ] N2 Low-stock alerts + reorder suggestions.
- [ ] N3 Retail perishable batch/expiry + FIFO (spinytails lots exist; retail-inventory expiry/FIFO separate).
- [ ] N4 Manager overrides for voids/refunds/discounts + returns/exchanges workflow at POS.
- [ ] N5 Abandoned-cart recovery (online).
- [ ] N6 Loyalty REDEMPTION at POS (points_balance exists; redeem flow missing).
- [ ] N7 Category-flexible tax engine (0% unprepared seafood/meat, standard elsewhere) — for when BSC sells non-exempt/prepared items. (VAT currently 0 global.)
- [ ] N8 Demand forecasting / analytics dashboards.  N9 Offline POS resilience+sync.  N10 self-service kiosk (future).  N11 recurring/standing orders (restaurants).  N12 reviews/ratings online.

## 🛍️ PHASE 2 (customer/commercial)
- [x] **G9 commercial side-by-side pricing** — PUSHED 298ee93. Dormant until Slice-3 fully ships (whoami is_commercial + /api/commercial/prices + business_accounts schema all still unpushed/un-run). Retail/anon unchanged. NOTE: this commit shipped the inert Slice-3 market CONSUMER; remaining Slice-3 to activate = whoami edit, commercial API, commercial-accounts page, founder tile re-add, RUN 1+2 schema.
- [x] **G11 cashier add-customer card** — BUILT local, compiles, NOT pushed. No migration. /cashier/add-customer (name/phone/email + live name-match dedupe → /api/pos/save-customer which dedupes by phone_e164/email) + cashier dashboard tile. Reuses save-customer + customer-search (cashier-gated).
- [x] **G10 customer-facing statements/balance** — BUILT local, compiles, NOT pushed. No migration. /api/account/statements (service-role, scoped to auth_user_id → customers → credit_invoices/payments/statements; reuses allocateStatement; signed PDF urls). /account/statements page (balance + invoices + payments + statement downloads) + /account tile. Customer self-serve AR view; data exists because G6 creates credit_invoices.
- [x] **G7 founder customer-approval queue** — BUILT local, compiles, NOT pushed. No migration. /api/founder/customer-approvals (GET un-handled early_access_signups + existing-customer flag; POST approve→create/match customer deduped by phone_e164/email + mark notified_at / dismiss→mark notified_at). /founder/customer-approvals page + tile. PHASE 2 COMPLETE on push.

## 🔧 PHASE 1 (connect dead-ends)
- [x] **G2+G3 Expense flow** — BUILT local, compiles, NOT pushed. Migration `docs/expenses/01-migration.sql` (expenses + image_url/status/approved_by/approved_at; FOUNDER RUNS FIRST). create-entity gains 'expense' target + OPS roles (cashier/andros_staff for purchase+expense) + else→else-if fix (was creating stray supplier on purchase). capture page Expense action. /founder/expenses approval queue (API+page+tile). Cashier photo receipt→pending expense→founder approve→accounting. ALSO fixed: cashier Scan Invoice→PO was 403-blocked (cashier not in create-entity allowlist) — now works (helps G4).
- [x] **G4 Receiving→inventory** — PUSHED 94ee8c5 (migration run). Live. Migration `docs/receiving/01-migration.sql` (purchase_invoices received_at/by; FOUNDER RUNS FIRST). create-entity attaches captured photo to purchase_invoices.image_urls + supplier_name. /api/founder/receiving (GET pending invoices+locations+products; POST → record_inventory_in per matched line [sku/qty/cost] → inventory_movements+current_stock w/ invoice#+photo; mark received + paid/outstanding). /founder/receiving page (per-line SKU datalist match + receive) + tile. Reuses record_inventory_in RPC + current_stock. Line→product match is manual (OCR has no SKU). New product still needs status=active to sell (separate gate).
- [x] **G5 phone-order handoff+payment** — BUILT local, compiles, NOT pushed. NO migration (existing orders cols). /api/phone-orders/[id]/collect (cashier/staff → mark paid+Confirmed + recordSaleFinancials, methods cash/card/wire/check); list adds payment_status; cashier page rewritten w/ tabs (Awaiting approval / Ready to collect / Collected) + Collect-payment method picker. Credit bill-to-account deferred to G6.
- [x] **G6 credit approval+terms** — BUILT local, compiles, NOT pushed. NO migration. /api/founder/credit (search + approve/set limit+terms → customers credit cols + approved_by/at). /founder/credit page + tile. /api/phone-orders/[id]/bill-credit (credit order → CREATE credit_invoice [the missing creator] + order on_account + credit_due_date + current_balance bump + recordSaleFinancials; limit-checked). cashier page: credit orders show "Bill to account". Connects credit→AR→statements (statement generator already reads credit_invoices).
- [x] **G18 sign-out top nav** + **G-pos shift-loader retry** — PUSHED 7d489dc. ✅ PHASE 1 COMPLETE. No migration. cashier/page.tsx: ⎋ Sign out in top nav (clearSignIn + signOut). pos/page.tsx: loadCashierSession retries getUser 4× (350ms) → no phantom "No shift" (Claff). PHASE 1 COMPLETE on push.

## 💳 PNP ONLINE PAYMENT WEBHOOK — NOW UNBLOCKED (James Turansky / PnP, 2026-06-29)
The ⭐ payment auto-confirm is unblocked for the ONLINE (Plug'n Pay) path. Mechanism (Smart Screens v2):
- In /api/payment/start: set `pb_success_url` = https://www.bscbahamas.com/api/webhooks/plug-n-pay AND `pb_transition_type=hidden` (direct server-to-server POST after auth). DO NOT set other callback URL fields (so a FAILED txn returns the customer to the billing page to retry). DO NOT use get/post transition (browser-dependent).
- /api/webhooks/plug-n-pay (currently a STUB) → make real: receives the transaction-reply form POST; on approved → mark order paid_in_full + recordSaleFinancials + raiseResale + notify; declined → hold/notify. MUST return HTML (gateway returns our HTML to the customer's browser → return a thank-you/redirect-to-track page).
- SIGNATURE (the blocker): enable "Response Verification Hash" in PnP merchant portal (Security Administration → Verification Hash) → shared verification key → Vercel env (e.g. PLUGNPAY_VERIFY_KEY); regenerate hash our side + compare. (Or whitelist PnP gateway IPs.) NO other API creds needed for the callback.
- FOUNDER SETUP: (1) enable Response Verification Hash + copy the key → give me to add as Vercel env; (2) request sandbox via accounting@plugnpay.com; (3) optional: Remote API (Query Transaction) for reconciliation needs a Remote Client Password + server-IP whitelist (PDF in Downloads).
- Docs: smart-screens-v2 §2 transaction-reply; callback-documentation (hidden). Replaces the "BLOCKED on Julian" note for the ONLINE path (RBC physical terminal still separate).
- ✅ BUILT on branch `pnp-hidden-callback` (pushed 2026-06-29) → Vercel PREVIEW deploy (sandbox PNP env on Preview scope; production main untouched). 3 changes: pb_transition_type=hidden; dropped pb_bad_card_url/pb_problem_url; htmlRedirect on all return-handler paths. SANDBOX-TEST the preview URL end-to-end (approved auto-confirms→fulfillment; declined retries) → then merge to main for production. Do NOT merge until verified.
- ⚡ VERIFY-LIVE (2026-06-29): MUCH already built. ENV VAR = `PNP_VERIFICATION_HASH_SECRET` (existing, used by payment/start + verifyResponseHash) — set to the Response Verification Hash key. lib/plugnpay/index.ts ALREADY sets pb_success_url/pb_bad_card_url/pb_problem_url + pb_response_message_type=querystring. lib/plugnpay/return-handler.ts `handlePnpReturn` ALREADY: hash verify + Query-Transaction 2nd factor + atomic paid-flip + recordSaleFinancials + raiseResale + assignDrivers (= auto-confirm to fulfillment on approved; no manual approval). RBC decline codes mapped (rbc-codes.ts). → REMAINING: (1) add `pb_transition_type: 'hidden'` to the form (server-to-server POST); (2) per James, DROP pb_bad_card_url + pb_problem_url so declined retries on PnP billing page (only success hits us); (3) make the success handler return HTML (meta/JS redirect to /order-confirmed) since the gateway returns our HTML to the browser. TEST IN SANDBOX (point PNP_GATEWAY_ACCOUNT + PNP_VERIFICATION_HASH_SECRET at sandbox) before live. Do NOT blind-push live-payment change.

## 📧 CUSTOMER AUTH EMAIL BROKEN (2026-06-29, blocks sandbox test) — DIAGNOSED
Signup-confirm + password-reset emails NOT arriving. ROOT CAUSE: these are sent by SUPABASE AUTH (app/login/page.tsx:140 signUp, :91 resetPasswordForEmail), NOT the app's Resend (which only does order/statement mail). Supabase Auth has no custom SMTP → built-in sender is rate-limited/spam-filtered → emails don't deliver.
FIX (founder, Supabase dashboard, ~5min): Authentication→Emails→SMTP = Resend (host smtp.resend.com, port 465, user 'resend', pass=RESEND_API_KEY, sender hello@bscbahamas.com). + URL Configuration: Site URL https://www.bscbahamas.com + Redirect URLs add /auth/confirmed + /reset-password (prod + preview URL). Fixes signup + reset uniformly.
IMMEDIATE TEST UNBLOCK: Supabase→Auth→Users→Add user (Auto Confirm) OR confirm SQL (/tmp/confirm_test_customer.sql, on clipboard) sets auth.users.email_confirmed_at.
OPTIONAL code fallback (offered): Resend-backed reset/signup via admin.generateLink + sendEmail. Build on founder go.
UPDATE 2 (spam + invalid links): code verified — auth redirect base = NEXT_PUBLIC_SITE_URL (lib/site-url.ts:26) → fallback window.origin then https://bscbahamas.com (NON-www) = mismatch risk. Links = ${base}/auth/confirmed + /reset-password (hash-token recovery). FIXES (config/DNS, founder; I can't read Supabase/Resend dashboards): (1) Vercel NEXT_PUBLIC_SITE_URL=https://www.bscbahamas.com (Prod+Preview); (2) Supabase URL Config: Site URL=https://www.bscbahamas.com + Redirect URLs https://www.bscbahamas.com/** , https://bscbahamas.com/** , https://*.vercel.app/** ; (3) Supabase custom SMTP=Resend; (4) DNS: Resend domain page SPF+DKIM+MX on bscbahamas.com + DMARC _dmarc TXT v=DMARC1;p=none. "Invalid/expired" likely = redirect allowlist mismatch OR email-scanner pre-fetch consuming one-time token → CODE FIX available: switch reset to token_hash /auth/confirm flow (build on go). Nothing to deploy unless token_hash fix chosen.
UPDATE 3 (BUILT, not pushed): app/auth/confirm/route.ts (token_hash verifyOtp → cookie session → redirect; pre-fetch-safe); middleware PUBLIC_ROUTES += /auth/ + /reset-password (were bounced to /login = real bug breaking links). login/page.tsx signup handler: existing-email (identities.length==0) → AUTO resetPasswordForEmail + message "already registered, we've sent a reset link"; confirm-required (!data.session) → info "check inbox"; new info banner. DIAGNOSIS signup-email-not-arriving = test email already exists (signUp sends nothing for existing) +/or Confirm-email toggle OFF +/or Confirm-signup template not saved. FOUNDER CONFIRM in Supabase: Confirm email ON; Confirm-signup template uses /auth/confirm token_hash; test w/ fresh email. EMAIL TEMPLATES (founder): both point to {{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=signup|recovery&next=/auth/confirmed|/reset-password.

## 🔴 NOW
- [x] **Statement Generator — PUSHED** (`9e1b8e4`, 2026-06-28). Migration ran clean; cron live; `/founder/statements` live. ⚠️ Commercial Accounts tile was REMOVED from founder/page.tsx for this push (route 404s until Slice 3 ships) — **RE-ADD the commercial tile when pushing Slice 3 (#5).** FOLLOW-UP: wire BankingBlock into receipt/invoice + thermal for credit/commercial.
- [x] **CloudPRNT — PUSHED** (`b6ffffc`, 2026-06-28). /api/cloudprnt live; printer URL setup pending founder. No POS button wired yet.
- [~] **Star CloudPRNT print endpoint** (mC-Print3 MCP31L · IP 192.168.50.199 · MAC 00:11:62:31:89:BC · 72mm). Printer polls our public HTTPS endpoint; no iPad/Mac driver. Verified live: no print_jobs/printers tables; order shape confirmed.
      - [x] migration `docs/printing/01-migration.sql` (printers + print_jobs + RLS + Nassau unit seed) — FOUNDER TO RUN
      - [x] `lib/star-markup.ts` formatter (Star Document Markup, 72mm, receipt/invoice/pick-ticket; VAT 0% line; Community Fund 0.5% line; trace QR)
      - [x] `app/api/cloudprnt/route.ts` (POST poll / GET job / DELETE confirm; MAC-gated; optional CLOUDPRNT_TOKEN; 90s stale-reclaim)
      - [x] `app/api/print/enqueue/route.ts` + `lib/print-receipt.ts` (is_staff-gated; default active printer)
      - [x] build-check PASS (both routes compiled; git 0/0 vs origin) — REPORTED, awaiting push + migration run
      - [ ] **PENDING:** founder runs `docs/printing/01-migration.sql`; enable CloudPRNT in printer web UI + set server URL; wire POS button (full-file swap of app/pos print handler — on go); first test-print verifies markup tags
- [x] **Cashier POS login bug** (Kerline) — DIAGNOSED (read-only, nothing changed). ROOT CAUSE: her `users.id` (47140fab-822d-4b34-9aa8-6d152b76b68d) ≠ her Auth uid; `get_my_user_record()` AND `is_staff()` are both SECURITY DEFINER matching `id = auth.uid()` → return nothing → "could not look up your account" + loop. RLS is NOT the cause (definer bypasses RLS). Also MISSING profiles row (profiles_by_email=0). Same class as Taylor. Fix SQL proposed (repoint users.id to auth uid + create profiles row) — HOLD for founder confirm. ROOT FIX (code): staff add-user API must set users.id = created auth uid + create profiles row, else every new cashier repeats this.
- [~] **POS "No shift" bug — CLAFF** (claff@bahamianseafoodconnection.com, id 6b5d0e24, cashier, active, id=auth uid; POS loads). Blocked at Confirm Sale: "Open a shift before ringing." Recurring. DIAGNOSED read-only: RLS allows self-read (not the block); RPC open_cashier_session sound (resumes/inserts on auth.uid, SECURITY DEFINER); location 'nassau' valid. LEADING SUSPECT: `loadCashierSession()` (app/pos/page.tsx:303) calls getUser() ONCE, no retry, bails silently if session not hydrated → cashierSession=null even with an open shift (pos-shell retries 4×; this loader doesn't). DATA VERDICT (founder ran diagnostic): 14 shifts all under correct uid, all cleanly opened+closed; LAST closed 2026-06-03; NO open shift for 25 days incl today. So no stranded shift / no id bug — just no open shift; guard correct. Code path intact (No-shift badge → modal → handleOpenShift → open_cashier_session, used 14×). NEXT: live test — Claff taps 🔴 No shift → enter cash → gold Open Shift; if 🟢 she's unblocked, if error capture text. DURABLE FIX (on go): loadCashierSession (pos/page.tsx:303) single getUser no-retry → bails silently → harden with retry like pos-shell. Change nothing until founder confirms.
- [~] **ALL-USERS login audit + prevention** (2026-06-28). AUDIT RAN: all 9 users OK (auth✓ id-match✓ profile✓). ONLY break = TAYLOR (taylor@bsc.com): has profiles + staff_roster row but NO users row → get_my_user_record/is_staff empty → "could not look up account" (her earlier fix didn't persist a users row). Repair SQL on clipboard (/tmp/taylor_fix.sql) — create users row keyed on auth uid (=profiles.id), valid location, must_change_password=false, keep her roster HR row. After run → re-run audit to confirm all OK. ONWARD (prevention): Taylor fix RAN (users_id 1097ffa9, has_profile=1) — re-run audit to confirm all OK. CODE PREVENTION BUILT (local, build-clean, NOT pushed) — 3 edits to app/api/staff/admin/route.ts create case: (1) profiles upsert now REQUIRED + error-visible (rollback on fail), (2) removed staff_roster success-fallback — users is source of truth (this was the Taylor/strand bug: users-insert fail → fell to staff_roster → reported success), (3) final verify BOTH users+profiles exist or hard-fail. Awaiting push. OPTIONAL DB-authoritative belt-and-suspenders: trigger sync_user_to_profile on public.users (offered, founder opt-in).
- [ ] **CLOUDPRNT_TOKEN hardening** — STILL OPEN (founder: no token for now, ship unsecured for live test; founder adds after printer verified). DO NOT scratch until founder says "scratch it". Where: Vercel env `CLOUDPRNT_TOKEN` + printer CloudPRNT password field.
- [x] **Weekly Customer Statement Generator** — BUILT (local, compiles, not pushed). All 5 decisions locked. Files: `lib/statements/allocate.ts` (oldest-first cascade + DUE/OVERDUE), `lib/statements/pdf.ts` (pdf-lib navy/light-blue, BSC logo only), `lib/banking-info.ts` + `components/BankingBlock.tsx` (single-source RBC banking block), `lib/statements/generate.ts` (cadence weekly/monthly via credit_terms + breach override), `app/api/cron/customer-weekly-statements` (2 schedules in vercel.json; pending only, never auto-send), `app/api/founder/statements` + `app/founder/statements` page (approve→send email/WhatsApp/print) + founder tile. FOUNDER TO RUN: `docs/statements/01-migration.sql`. FOLLOW-UP: wire BankingBlock into receipt/invoice pages + thermal markup for credit/commercial (full-file swaps).
- [x] **Resolve Taylor's login** — done (auth-id repair ran; Taylor reaches /pos).

## ✅ READY TO PUSH — #2 Invoice auto-add + slow-movers (awaiting founder go)
- [x] `/api/founder/slow-movers` + `/founder/slow-movers` page + dashboard tile
- [x] `/api/supplier/invoice-auto-add` (match + review-queue create via bulk-add)
- [x] `/api/supplier/invoice-import-data` (picker data)
- [x] `/founder/invoice-import` review page (pick invoice + supplier → preview → add)
- [x] founder dashboard nav tiles (slow-movers + invoice-import)
- [ ] **push #2** (all of the above together) — awaiting go

## 🛰️ PHASE 4
- [x] **G8 WhatsApp intake** — PUSHED 8be2cfb. whatsapp_messages migration (`docs/whatsapp/01-migration.sql`) FOUNDER RUNS + Twilio inbound webhook → /api/webhooks/whatsapp-inbound. Cashier 💬 card (live poll, add-customer dedupe, new phone order, wa.me QR). Signature-validated. Realtime = poll v1.
- [x] **G13 boat self-signup** — PUSHED a91ebf5. No migration. apply endpoint stores details + cert on pending supplier (+ accepts vessel_registration_number, cert_url). /api/founder/supplier-applications (GET pending; POST activate/reject) + /founder/supplier-applications page (approve→invite creates login+activate / reject) + tile. Deferred: cert-upload widget on public signup page (endpoint accepts URL; post-login upload at /lobster-intake works).
- [x] **NAV BUG FIX** — PUSHED ce3574d. (2026-06-29 founder) staff "back to dashboard" → marketplace. ROOT CAUSE: AppShell STAFF_ROLES missing supplier_handler/processor/operations/driver/right_hand/strategist/supervisor/qc_staff/partner_us → bounced to /market (line 170) + fell to CUSTOMER_NAV (Home/Shop→/market). FIX (app/AppShell.tsx): STAFF_ROLES covers all internal staff; module ROLE_HOME map; navItems gives every staff role 🏠 Dashboard→their home + never CUSTOMER_NAV; back button reuses ROLE_HOME. Customer marketplace flow untouched. BUILT, NOT pushed. (Related: per-page spinytails `!STAFF_ROLES→/market` guards use local sets — verify they include processor etc. if issues persist.)
- [ ] consolidated P&L/cash-flow · (PnP online payment webhook — see top, awaits founder verify key).

## 🏭 PHASE 3 (supplier catalog + processing plant)
- [x] **G15 freezer temp card** — BUILT local, compiles, NOT pushed. No migration. /api/spinytails/freezer-temp (GET today+counts; POST → spinytails_temperature_logs, within_limit auto vs ceiling). /spinytails/freezer-log page (blast+holding, 3×/day progress, excursion flags) + dashboard NavTile. ⚠️ Ceilings blast≤0°F / holding≤10°F = DEFAULTS, founder to confirm HACCP limits (1-line const).
- [x] **G19 Spiny Tail ORDERS card on processor dashboard** — BUILT local, compiles, NOT pushed. No migration. /api/spinytails/orders (active orders last 21d with is_bsc_processed lines across channels; resolves is_bsc_processed via stamped line OR products). /spinytails/orders page (grouped Phone/Online/Nassau POS, items to process, paid status, 🖨 Pick ticket link per order) + dashboard task card. Founder confirmed: pick ticket = the printable order on processor dashboard. ✓
- [x] **G16 freezer-removal reasons** — BUILT local, compiles, NOT pushed. No migration. Freezer-removal flow already existed in /spinytails/processing (weight + tray/rack/blast + received-vs-removed reconciliation); G16 aligned PURPOSES to founder spec: deveine_sleeve_blast (default), pos_nassau, online_order, pos_andros, wholesale, export, other_processing, sampling, qc, disposal. purpose is free text (no enum).
- [x] **G14 processor receiving fields** — BUILT local, compiles, NOT pushed. Migration `docs/spinytails/02-receiving-fields.sql` (lot_intakes + reject_ratio_pct, harvest_positions; FOUNDER RUNS FIRST). Receiving page: reject ratio input, 3–4 harvest GPS positions (📍 capture), vessel color_tag surfaced (dropdown + "color string" on select). receive API stores both. Sulfite already captured via CCP-1 qc_results.sulfite_ppm (no dup). Boat-cert-at-receiving = vessel registration already on supplier record (vessel_registration_doc_url) — surfacing on spinytails receiving deferred (cert lives on suppliers, vessels link separate).
- [x] **G17 CITES quota** — BUILT local, compiles, NOT pushed. Migration `docs/spinytails/03-cites-quota.sql` (quota_tracking table + RLS via bsc_is_internal_staff; FOUNDER RUNS FIRST). /api/founder/cites-quota (GET ceilings + used computed live from spinytails_shipments weight in period; POST upsert/delete). /founder/cites-quota page (ceiling per species/season + utilization bar 85%amber/100%red) + tile. NOTE: used = total export shipment weight in period (lobster-only op); per-species attribution = refinement if needed.
- [ ] G12 catalog OCR+bucket (blocked: bucket SQL + external files) — last Phase-3 item.

## 🦞 PROCESSOR RECEIVING + FREEZER + HACCP (2026-06-28 founder spec) — superseded by Phase 3 items above
Processor home (/spinytails) = "Receiving Products". Build against existing spinytails_* infra (14 tables, lot STPC-YYYYMMDD-VV-NN, 5 CCPs) + lobster-intake + vessel registration on supplier record. VERIFY live schema before building.
- [ ] Boat registration certificate: upload at receiving, OR if already on the boat-name file, only yearly renewal/replacement (auto-fill from supplier/vessel file).
- [ ] Receiving form fields: product temperature, weights, sulfite level, reject ratio per bag.
- [ ] 3–4 fishing GPS positions from the trip (where harvested).
- [ ] Auto-create BATCH number matching this lot → lot number encodes boat + captain info.
- [ ] On receive: print LABEL from label printer to put on the bin; few-bags case = color-coded string tying receiving product together so no boat product mixes.
- [ ] "Remove product from freezer" action w/ REASON: deveining+sleeving for blast freeze / BSC Nassau POS order / online order / other processing stage (freezer movement log).
- [ ] Dashboard CARD: boat blast freezer + holding freezer temperature log — record temp 3×/day per freezer.
- [ ] All SOP / HACCP / SSOP accessible from processor dashboard.

## 🔐 ALL-ROLES ACCESS PASS (2026-06-28 founder: "ensure all users have access to cover all their assigned duties based on their roles")
- [ ] Audit EVERY role's ROLE_ROUTES vs its dashboard nav hrefs + duties; fill gaps so no tile bounces. cashier + supplier_handler done (#9). Remaining: processor, operations, driver, manager, andros_staff, strategist, right_hand, supplier, partner_us.

## 🧭 ROLE-BASED ACCESS + NAV (2026-06-28, founder batch) — PUSHED 2cf6f06
- [x] **A. Universal "Go to Dashboard"** — AppShell sticky "← Dashboard" now routes each role to THEIR home (ROLE_HOME map, mirrors middleware) instead of sending non-admins to /dashboard (which bounced). `app/AppShell.tsx`.
- [x] **B. Supplier handler access** — already could VIEW all suppliers + reach /supplier (no RLS/route block). NEW: `canManageSuppliers()` in lib/role.ts (founder/co_founder/control_admin/manager/**supplier_handler**); `app/supplier/page.tsx` canEdit now uses it → supplier_handler can add/edit suppliers + products, upload pricelist (bulk), extract, toggle online. (Was founder-only via canLock.)
- [~] **C. Cashier access** — sees orders ✓ (was working); supplier invoice photo → PO ✓ (/documents/capture target 'purchase'); POS Nassau end-to-end ✓ (cashier in POS ALLOWED_ROLES); **NEW**: middleware adds `/expenses` + `/phone-order` to cashier; cashier dashboard tiles added (Phone Order→/phone-order, Orders, Scan Invoice, Expenses, PO). Phone-order create endpoint already allows cashier. ⚠️ GAP: photo→EXPENSE record not supported — capture has no 'expense' target (only supplier/fisherman/customer/purchase). Cashier can reach /expenses but photo→expense needs an 'expense' target added to /documents/capture + API (FOLLOW-UP). Optional: enforce primary_location in POS (deferred — could block).
      ↳ Files: app/AppShell.tsx, lib/role.ts, app/supplier/page.tsx, middleware.ts, app/cashier/page.tsx. Build-clean. No push without founder go.

## 🟢 QUEUED (in order)
- [~] **Directives System** (founder→staff assignment board) — handoff 2026-06-27. Decisions: body_cr / all-4-landings / defer auto-fire.
      - [x] STEP 1 verify-live + STEP 2 connector mapping (reported)
      - [x] STEP 3a migration → `docs/directives/01-migration.sql` (helper now plpgsql, paren-safe) — FOUNDER TO RUN (first attempt pasted the handoff block by mistake)
      - [x] STEP 3b `/founder/directives` composer (API + page) — built local, compiles. Task/duty, 3-lang body, role/location/user targets, seen/done counts, close. Audit→ai_writes.
      - [x] STEP 3d task=1 instance / duty=current-cycle instance on create (cycleKey helper)
      - [x] STEP 3c staff feed: `/api/directives/feed` (targeting + lazy duty-cycle + tri-lingual via profiles.language + seen/done) + `components/directives/MyDirectives.tsx` (fail-silent) embedded on /founder, /cashier, /spinytails, /supplier-handler
      - [ ] **BLOCKER:** migration not yet applied live (0 tables — pasted wrong text twice). Re-deliver as fenced block. Then push composer + feed.
- [ ] **FIX 2** — auto-return to dashboard after a photo upload (brief "Photo attached to PO #__" confirm).
- [ ] **FIX 3** — photo routing/filing:
      • supplier invoice → Purchase Orders
      • fishing-boat receipt (uploaded by a **processor**) → PO routed to **Spiny Tail** → starts intake + traceability (lot `STPC-YYYYMMDD-VV-NN`); **boat registration auto-pulled from the fisherman/boat (vessel) file**.
      ↳ NEEDS 2 answers: (a) `purchase_orders` vs `purchase_invoices`? (b) how is a boat receipt distinguished — uploader role=processor, or a doc-type pick?
- [ ] ⭐ **PACKAGE 3 — Payment Terminal Integration (PRIORITY)** — real-time RBC terminal (MID 024150/TID 02415004) + Plug'n Pay webhooks → auto-confirm paid orders; declined/timeout → hold "Payment Pending" + `/founder/payment-approvals` manual override. New: `orders.payment_approval_status` + `payment_webhook_received_at` + `payment_webhooks` table + order-confirm gating + 2 webhook endpoints. ⚠️ BLOCKED on Julian (webhook docs/creds/signature algo/sandbox); migration + manual page + gating buildable now, signature validation NOT. Test staging first. NEVER auto-process unpaid.
- [ ] **Supplier Catalog Image Pipeline** (spec 2026-06-28) — private `supplier_catalogs` bucket (is_staff RLS) → extract images from PDFs/email → match to pricelist by Item ID → `{supplier}_{product_id}` upload → auth URLs → "Image URL" column. NDC first (207 lines, 19 images). ⚠️ source files EXTERNAL — founder must provide. Reusable code buildable without them. Verify bucket + report first batch before push.
- [ ] `get_my_user_record` hardening (fallback to staff_roster) — recommended, prevents recurrence.

## ⏸ DEFERRED (blocked / by date)
- [ ] International **audit log + export docs** — until Spiny Tail has real processing data (lots/temps/certs).
- [x] **Slice 3** commercial marketplace — BUILT (local, awaiting push): /founder/commercial-accounts (provision client → login + tier), /api/commercial/prices (cost×1.14 server-side), whoami extended (is_commercial+tier), /market shows tier pricing + banner. FOLLOW-UP: commercial order summary view (open/paid/shipped); username-only login wrapper.
- [ ] Phase 2: #3 6% fix (no live target — confirmed), #5 Slice 2 Spiny-Tail queue, #6 velocity pricing, #7 tri-lingual.

## 🧰 FOUNDER TO RUN (SQL — not code)
- [ ] Taylor login repair (clipboard now).
- [ ] RUN 1 + RUN 2 Phase-1 schema (commercial tiers, `business_accounts`, `quota_tracking`) — unblocks commercial + international accounts + the export gate.
- [ ] (optional) `get_my_user_record` staff_roster fallback.

## 🚀 SHIPPED (recent)
- Phone orders (entry→approval→print) + Slice 1 paid-orders + Nassau daily-sales filter.
- International export gate + export-only `/market` filter + banner (dormant until RUN 2 + accounts).
